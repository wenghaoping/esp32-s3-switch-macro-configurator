# Vue 控制台与板载宏循环说明

当前 WebUI 使用 Vue 3、Vite、Vue Router 和 Pinia。启动方式：

```bash
npm install
npm run serve
```

使用桌面版 Chrome 或 Edge 打开 <http://localhost:5173>。生产构建使用：

```bash
npm run build
```

## 页面

- `#/`：首页与当前设备摘要。
- `#/control`：连接、运行、停止、任务进度和“需要补一刀”虚拟手柄。
- `#/scripts`：十二个宏槽位与一套最多五项的宏循环。
- `#/scripts/:slot/edit`：编辑一轮完整脚本。
- `#/recorder`：使用中文虚拟手柄录制并写入 Flash。
- `#/device`：双 USB 接线、GPIO 和配置备份。

应用只创建一个串口服务实例。站内切换路由不会关闭端口；整页刷新后可使用浏览器已经授权
的端口重新连接。

## 脚本与任务的区别

一个脚本表示“一轮完整动作”。在控制页直接启动脚本时，它会持续循环到用户停止。

宏循环用于精确控制多个宏的顺序和次数。当前开发板只保存一套宏循环，例如：

```text
天埠罗巢穴刷武器 × 100
→ 杏棱巢穴刷钱 × 20
→ 武器分解 × 1
→ 返回天埠罗巢穴刷武器进行大循环
```

任务最多 5 项；单项次数为 `1～10000`，完成后等待为 `0～600000 ms`。同一个槽位可以重复
选择。任务只能引用 ESP32 当前有效的脚本槽位：Flash 保存版本优先于同槽位的 C++ 内置版本。
网页草稿必须先写入 Flash 才能加入任务。

旧版“起始步骤/结束步骤”、范围测试和单宏步骤分段已经停用。2.0 只接受明确标注新版格式与
版本的 JSON；旧 JSON 或包含 `plan.segments` 的文件会直接拒绝，不兼容也不迁移。

## 板载 RGB 状态灯

板载 RGB 灯由固件自动显示状态：启动白色常亮、空闲绿色常亮、单宏运行青色常亮、任务方案运行紫色常亮、上传配置蓝色双闪、
Flash 写入黄色快闪并在成功后绿色双闪、GPIO 触发白色短闪、错误
红色三闪循环。默认亮度为 10/255，可通过串口命令 `LED_BRIGHTNESS 0～255` 临时调整；重启后
恢复编译时的默认亮度（当前为 10/255）。

## 板载运行

任务通过 `TASK_BEGIN`、`TASK_META`、最多 5 条 `TASK_ENTRY` 和带校验和的 `TASK_COMMIT`
事务式写入。只有完整校验通过后才替换旧方案。启动任务后，网页或 USB-UART 断开不会停止
ESP32；使用网页停止、`STOP`、Stop GPIO、复位或断电结束运行。

## 模拟界面

开发时可打开 <http://localhost:5173/?mock=1#/scripts> 使用模拟串口检查内置脚本、五项上限和
任务保存交互，不需要连接 ESP32。
